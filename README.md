# Workspace Overview

このワークスペースは 2 つの sibling project を持つ。

- `iSensorium`
  Xperia 5 III 上の多種センサ同時記録基盤
- `iDevelop`
  Codex Dashboard 系の companion project

各 project はそれぞれの `AGENTS.md` と `docs/index.md` / `develop/index.md` を入口とする。
## 2026-03-14 Update

- sibling projects now include `coreCamera/` in addition to `iSensorium/` and `iDevelop/`
- `coreCamera/` is reserved for isolated `Camera2 + ARCore Shared Camera` replacement-camera work
